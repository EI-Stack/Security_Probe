package oam.security.util.override;

public class BasicDataSourceWithAddingDeregisterDriver // extends BasicDataSource
{
	/*
	@Override
	public synchronized void close() throws SQLException
	{
		DriverManager.deregisterDriver(DriverManager.getDriver(this.url));
		super.close();
	}
	 */
}
