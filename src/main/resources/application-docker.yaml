#======================================================================================================================#
# 自定義參數
#======================================================================================================================#
solaris:
  #====================================================================================================================#
  # 自定義：Server Connection Configuration
  #====================================================================================================================#
  server:
    free5gc:
      http: 
        url: ${K8S_URL}
  prometheus:
    url: ${PROMETHEUS_URL}
    namespace: ${PROMETHEUS_NAMESPACE}
  session:
    dn-service: ${DN_SVC}
    dn-socketPort: ${DN_SOCKET_PORT}
    manage_service: ${MANAGE_SVC}
#======================================================================================================================#
# Spring
#======================================================================================================================#
spring: 
  #====================================================================================================================#
  # Data Source
  #====================================================================================================================#  
  datasource:    
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}